import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    root: {
        width: '200px',
        minWidth: '100%',
        marginTop: '5rem',
    },
    pos: {
        marginBottom: 12,
    },
    input: {
        marginBottom: 20,
    },
    typo: {
        marginTop: 10,
        marginBottom: 10,
    },
    header: {
        marginBottom: 25,
    },
})

export default useStyles
