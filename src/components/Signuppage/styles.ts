import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    root: {        
        width: '200px',
        minWidth: '100%',
        marginTop: '5rem'
    },
    pos: {
        marginBottom: 12,
    },
    textfield: {
        marginBottom: 25,
        
    },
    
})
export default useStyles
